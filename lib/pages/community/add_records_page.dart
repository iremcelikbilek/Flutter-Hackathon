import 'package:flutter/material.dart';

class AddRecordsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Kayıt Ekle"),),
      body: Center(child: Text("Kayıt Ekleme"),),
    );
  }
}
