import 'package:flutter/material.dart';

class RecordsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Kayıtlar"),),
      body: Center(child: Text("BÜTÜN KAYITLARIN LİSTESİ"),),
    );
  }
}
