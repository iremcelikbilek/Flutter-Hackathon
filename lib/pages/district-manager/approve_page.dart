import 'package:flutter/material.dart';

class ApprovePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Onay"),),
      body: Center(child: Text("Onay Sayfası"),),
    );
  }
}
